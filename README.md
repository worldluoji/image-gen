# IMAGE GEN

核心功能：调用大模型生成图片，支持文生图、图生图。

一期仅支持 MiniMax H3。

## 功能

- **文生图**：输入文字描述，选择模型（`image-01` / `image-01-live`）、宽高比、生成数量（1-9）与画面风格，调用 MiniMax `image_generation` 接口生成图片。API Key 仅存于服务端，浏览器不直接接触密钥。
- **风格选择**：预设 16 种常用画风——电影 / 写实 / 日漫 / 国风动漫 / 3D 卡通 / 水彩 / 油画 / 素描 / 中国水墨 / 像素艺术 / 赛博朋克 / 复古胶片 / 扁平插画 / 儿童绘本 / 浮世绘 / 概念艺术，也可自定义输入（≤100 字符）。风格会拼接进 prompt 发送给模型（上游 API 的 `style` 参数仅支持 4 种固定画风且只对 `image-01-live` 生效，故未采用）。
- **图生图**：在同一表单上传参考图（JPG / PNG，< 10MB，建议人物主体清晰），即以 `subject_reference` 调用同一接口进行人物主体参考生成；不传图则为文生图。
- **图片持久化与下载**：生成成功后服务端自动将图片落盘到 `public/generated/`（上游 URL 仅 24 小时有效，落盘后永久可用），每张结果图可直接下载。
- **生成历史**：每批生成记录（描述、参数、图片）持久化到 `data/history.json`，页面底部展示历史；可「载入参数」回填表单，或点击历史缩略图将其作为参考图。最多保留最近 50 批。
- **快速再创作**：结果区支持「作为参考图」（服务端自动将本地图转为 Base64 传给上游）与「再创作一批」（一键复用上一批全部参数重新生成）。
- **过程反馈**：生成中按所选数量与宽高比显示骨架占位；部分失败时明确提示成功/失败张数。

## 快速开始

1. 环境要求：Node.js >= 22，pnpm
2. 安装依赖：`pnpm install`
3. 配置密钥：复制 `.env.example` 为 `.env.local`，填入 `MINIMAX_API_KEY`
4. 开发运行：`pnpm dev`，打开 http://localhost:3000

## 常用命令

| 命令 | 说明 |
| --- | --- |
| `pnpm dev` | 开发服务器 |
| `pnpm build` | 生产构建 |
| `pnpm lint` | 语法检查 |
| `pnpm test` | 单元测试（Vitest，表格驱动） |

## 项目结构

- `app/page.tsx` — 文生图表单页面（结果卡片、历史区、骨架屏）
- `app/api/generate/route.ts` — 生成接口 Route Handler（参数校验、密钥读取、错误映射、图片落盘、写历史）
- `app/api/history/route.ts` — 历史记录查询接口
- `lib/minimax.ts` — MiniMax 客户端（请求构造 / 响应解析 / fetch 封装）
- `lib/storage.ts` — 图片落盘与历史持久化（本地参考图转 Base64）
- `lib/minimax.test.ts`、`lib/storage.test.ts` — 单元测试

## 说明

- 上游返回的图片 URL 有效期为 24 小时；服务端会在生成成功后自动落盘到 `public/generated/`（已 gitignore），单张落盘失败时回退使用远端 URL。
- 生成超时时间为 120 秒。
- 「作为参考图」使用本系统生成的图片时，由服务端读取落盘文件转为 Base64 Data URL 传给上游（上游无法访问本地地址）。
